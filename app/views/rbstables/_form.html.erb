<%= form_for(@rbstable) do |f| %>
  <% if @rbstable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rbstable.errors.count, "error") %> prohibited this rbstable from being saved:</h2>

      <ul>
      <% @rbstable.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
