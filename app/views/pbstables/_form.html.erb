<%= form_for(@pbstable) do |f| %>
  <% if @pbstable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pbstable.errors.count, "error") %> prohibited this pbstable from being saved:</h2>

      <ul>
      <% @pbstable.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %><br>
    <%= f.label "Project" %><br>
    <%= f.collection_select :project_id, Project.all, :id, :name, {multiple: false} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
1

Edit | Back